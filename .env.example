# Root .env — shared by docker-compose for the PostgreSQL container.
# The apps have their own .env files (see apps/backend/.env.example and
# apps/frontend/.env.example).

DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_NAME=klontong_admin
DB_PORT=5432
